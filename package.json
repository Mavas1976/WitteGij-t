{
  "name": "witte-gijten",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "sirv dist --host --single"
  },
  "devDependencies": {
    "vite": "^5.2.0"
  },
  "dependencies": {
    "sirv-cli": "^3.0.1"
  }
}
